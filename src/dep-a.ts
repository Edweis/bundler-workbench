export const depABis = (str: string) => {
  if (str === "1") {
    return 12;
  }
};
export const depA = (str: string) => {
  if (str === "1") {
    return 12;
  }
};

export class Foo {
  bar() {
    let x = 12;
    x = x + 2;
  }
}
